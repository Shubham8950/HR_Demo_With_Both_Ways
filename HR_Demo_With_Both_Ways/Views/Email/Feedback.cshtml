
@{
    ViewBag.Title = "Feedback";
}

<!--  jQuery -->
<script type="text/javascript" src="https://code.jquery.com/jquery-1.11.3.min.js"></script>

<!-- Isolated Version of Bootstrap, not needed if your site already uses Bootstrap -->
<link rel="stylesheet" href="https://formden.com/static/cdn/bootstrap-iso.css" />
<script type="text/javascript" src="https://ajax.googleapis.com/ajax/libs/jquery/1.8.3/jquery.min.js"></script>
<!-- Bootstrap Date-Picker Plugin -->
<script type="text/javascript" src="https://cdnjs.cloudflare.com/ajax/libs/bootstrap-datepicker/1.4.1/js/bootstrap-datepicker.min.js"></script>
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap-datepicker/1.4.1/css/bootstrap-datepicker3.css" />

    <div>
        <div class="form-group">
            <label for="trainingType">Training Type</label>
            <select class="form-control" id="trainingType">
                <option>Finance</option>
                <option>Computer</option>
                <option>Analysis</option>
            </select>
        </div>

        <div class="form-group">
            <label for="exampleFormControlInput1">Emp Id</label>
            <input type="email" class="form-control" id="empId" placeholder="name@example.com">
        </div>

        <div class="form-group">
            <label for="exampleFormControlInput1">Start Date</label>
            <input class="form-control" id="startDate" data-date-format="mm/dd/yyyy" placeholder="MM/DD/YYY" type="text">
        </div>

        <div class="form-group">
            <label for="exampleFormControlInput1">End Date</label>
            <input class="form-control" id="endDate" data-date-format="mm/dd/yyyy" placeholder="MM/DD/YYY" type="text">
        </div>

        <div class="form-group">
            <label for="exampleFormControlInput1">Description</label>
            <input type="text" class="form-control" id="description" placeholder="Description">

        </div>

        <div class="form-group">
            <!-- Submit button -->
            <input type="button" id="btnFeedback" class="btn btn-primary " value="Submit"/>
        </div>
    </div>
 
    <script>
    $('#startDate').datepicker({
        format: 'mm/dd/yyyy',
        startDate: '-3d'
    });
    $('#endDate').datepicker({
        format: 'mm/dd/yyyy',
        startDate: '-3d'
    });
        function GetAjaxDataPromise(url, postData) {
            debugger;
            var promise = $.post(url, postData, function (promise, status) {
            });
            return promise;
        };
    $(function () {
        $("#btnFeedback").click(function () {
             //fifth

            var promises = GetAjaxDataPromise("/Email/Feedback", {
                TrainingType: $("#trainingType").val(),
                EmpId: $("#empId").val(),
                StartDate: $("#startDate").val(),
                endDate: $("#endDate").val(),
                Description: $("#description").val()
            });
            promises.done(function (response) {
                debugger;
                alert("Mail Sent");
            });
           
        });
    });
    </script>
